# Input parameters:
# - api_version
# - name

rest_calls:
  - path: '/v{{ api_version }}/containers/{{ name }}/stop'
    method: 'POST'
    headers:
      Content-type: 'application/json'
    payload:
    response_format: raw
