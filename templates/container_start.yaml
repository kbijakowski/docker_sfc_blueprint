# Input parameters:
# - api_version
# - name

rest_calls:
  - path: '/v{{ api_version }}/containers/{{ name }}/start'
    method: 'POST'
    headers:
      Content-type: 'application/json'
    response_format: raw
