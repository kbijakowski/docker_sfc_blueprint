# Input parameters:
# - api_version
# - name

rest_calls:
  - path:  '/v{{ api_version }}/networks/{{ name }}'
    method: 'DELETE'
    headers:
      Content-type: 'application/json'
    payload:
    response_format: raw

