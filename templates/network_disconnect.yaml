# Input parameters:
# - network_name
# - container_name

rest_calls:
  - path: ''
    method: 'POST'
    headers:
      Content-type: 'application/json'
    payload:
    response_format: json
    response_translation:
      data: [data]
    response_expectation: